[package]
name = "clash_verge_service_ipc"
version = "2.6.6"
edition = "2024"
authors = ["Tunglies"]
license = "GPL-3.0"

[lib]
name = "clash_verge_service_ipc"
path = "src/lib.rs"

[[bin]]
name = "nexus-service"
path = "src/bin/service.rs"
required-features = ["standalone"]

[[bin]]
name = "nexus-service-install"
path = "src/bin/install_service.rs"
required-features = ["standalone", "client"]

[[bin]]
name = "nexus-service-uninstall"
path = "src/bin/uninstall_service.rs"
required-features = ["standalone"]

[[bin]]
name = "mock_binary"
path = "src/bin/mock_binary.rs"
required-features = ["test"]

[[bin]]
name = "crash_binary"
path = "src/bin/crash_binary.rs"
required-features = ["test"]

[[bin]]
name = "owner_lock_holder"
path = "src/bin/owner_lock_holder.rs"
required-features = ["standalone", "test"]

[[bin]]
name = "service-integration-driver"
path = "src/bin/service_integration_driver.rs"
required-features = ["client"]

[features]
development-channel = []
standalone = [
    "kode-bridge",
    "kode-bridge/client",
    "http",
    "tracing",
    "tracing-subscriber",
    "tokio",
    "anyhow",
    "once_cell",
    "serde_json",
    "clash_verge_logger",
    "flexi_logger",
    "log",
    "response",
    "dep:platform_lib",
    "dep:nix",
    "dep:windows-sys",
    "compact_str",
]
client = [
    "kode-bridge/client",
    "anyhow",
    "log",
    "tokio",
    "once_cell",
    "serde_json",
    "response",
    "compact_str",
    "dep:platform_lib",
    "dep:windows-sys",
]
response = []
test = [
    "anyhow",
    "dep:platform_lib",
    "dep:windows-sys",
]
default = []


[dependencies]
kode-bridge = { version = "0.5.1", features = ["server"], optional = true }
once_cell = { version = "1.21", features = ["parking_lot"], optional = true }
anyhow = { version = "1.0", optional = true }
http = { version = "1.3", optional = true }
serde = { version = "1.0", features = ["derive"] }
serde_json = { version = "1.0", optional = true }
sha2 = "0.11"
strum = { version = "0.28" }
strum_macros = { version = "0.28" }
tokio = { version = "1.52", features = [
    "process",
    "io-util",
    "rt-multi-thread",
    "macros",
    "signal",
    "fs",
    "time",
], optional = true }
tracing = { version = "0.1", optional = true }
tracing-subscriber = { version = "0.3", optional = true }
clash_verge_logger = { git = "https://github.com/clash-verge-rev/clash-verge-logger.git", version = "0.2.0", optional = true }
flexi_logger = { version = "0.31", optional = true }
log = { version = "0.4.28", optional = true }
compact_str = { version = "0.10", features = ["serde"], optional = true }
url = "2.5"

[target.'cfg(target_os = "macos")'.dependencies]
sysproxy = { git = "https://github.com/clash-verge-rev/sysproxy-rs.git", branch = "main", default-features = false }

[target.'cfg(unix)'.dependencies]
platform_lib = { package = "libc", version = "0.2", optional = true }
nix = { version = "0.31", features = ["user"], optional = true }

[target.'cfg(windows)'.dependencies]
platform_lib = { package = "windows-service", version = "0.8", optional = true }
windows-sys = { version = "0.61.2", optional = true, features = [
    "Win32_Foundation",
    "Win32_Security",
    "Win32_Security_Authorization",
    "Win32_Storage_FileSystem",
    "Win32_System_Com",
    "Win32_System_Pipes",
    "Win32_System_Threading",
    "Win32_UI_Shell",
] }

[dev-dependencies]
kode-bridge = { version = "0.5.1", default-features = false, features = [
    "server",
    "client",
] }
serial_test = "4.0"

[profile.release]
panic = "abort"
lto = "thin"
opt-level = 3
codegen-units = 1
